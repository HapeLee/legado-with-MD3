name: Release APK

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: write

jobs:
  release:
    uses: ./.github/workflows/auto-release.yml
    with:
      publish: true
    secrets: inherit
