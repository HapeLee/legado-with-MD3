# Feature catalog（初始提案）

> 本表用于统一 owner 与 canonical name，不表示模块已经存在。每个条目在首次迁移前仍需做调用方和依赖审计。

| 当前主要位置                                   | Canonical owner             | 目标类型                          | 备注                                                                           |
|------------------------------------------|-----------------------------|-------------------------------|------------------------------------------------------------------------------|
| `ui/main/MainActivity*`、全局 route graph   | `app/android`               | app host                      | 保留导航、DI、launcher 与全局 shell 聚合，不建 `feature-main`                              |
| `ui/main/bookshelf`                      | `feature/bookshelf`         | Feature                       | 书架列表、分组入口和选择状态；书籍数据仍经 core data/domain                                       |
| `ui/main/home`、`ui/main/homepage`        | `feature/home`              | Feature                       | 先确认两者职责后合并 canonical name，禁止继续新增第三套首页包                                       |
| `ui/main/explore`、`ui/book/explore`      | `feature/explore`           | Feature                       | 区分入口容器与探索结果子流程，避免两个独立 impl 互相依赖                                              |
| `ui/book/info`                           | `feature/book-info`         | Feature                       | 中型样板候选，但**需先分解再整包迁**：主包 VM 直连 8 个 Repository + coil3/rhino，Screen 含 WebView，整包搬会一次引入多重反依赖。**edit 子包已先行 nav3 化（2026-09-10）**：薄宿主 `BookInfoEditActivity` 删除，改走同栈 `MainRouteBookInfoEdit`（`BookInfoEditRouteScreen` 留 `:app`）；保存成功由 `BookInfoEdited` 经 `ResultEffect` 回传触发 `onInfoEdited`（原 `setResult(RESULT_OK)` 语义，未保存直接返回不发），页内四处「人物/关系/知识/事件」入口从 `startActivity(MainIntent.createXxx...)` 改为栈内导航，随之 `StartActivityContract` 成死代码删除。软键盘由原 `adjustResize\|stateHidden` 变为继承 MainActivity 的 `adjustResize`（少了 `stateHidden`，真机需确认进页不会自动弹键盘）。 |
| `ui/book/search`、`ui/book/searchContent` | `feature/book-search`       | Feature                       | 全局搜书与正文搜索可先作为同域子流程，稳定后再评估拆分                                                  |
| `ui/book/toc`、`ui/book/toc/rule`、`ui/book/toc/rule/preview` | `feature/table-of-contents` + `feature/txttocrules` | Feature + platform adapter | **拆三域（2026-09-09 审计）+ nav3 收口（2026-09-10）**：① `rule` 子域 = TXT 目录规则管理，**`:feature:txttocrules` 已建**：`TxtRuleScreen`（含 `TxtRuleRouteScreen`）/`TxtTocRuleContract`/`TxtTocRuleViewModel` 迁入 `io.legado.app.feature.txttocrules`（3 文件 + 26 R.string×4 语言）；VM 的「导入内置规则」原直连 `DefaultData`（appDb+assets，不可下沉）→ 抽 `:core:viewmodel` 契约 `BuiltInRulesImporter`（impl `AndroidBuiltInRulesImporter` 留 app，Koin 注入）。② `rule/preview` 子域（TxtTocRulePreviewScreen/Contract/VM）依赖 app 的 `model.localBook.LocalBook`（本地书解析）→ **platform island，Screen/VM/Contract 留 `:app` 不迁**。③ `toc` 主域（TocScreen/ViewModel，书籍目录页 2280 行）属阅读主链，**留 `:app`**。**宿主已全部 nav3 化**：原独立 Activity 薄宿主 `TxtTocRuleActivity` / `TocActivity` / `TxtTocRulePreviewActivity` 于 2026-09-10 删除，改走同栈 `MainRouteTxtTocRule` / `MainRouteToc` / `MainRouteTxtTocRulePreview`（nav3 1.2.0-beta01），经 `MyScreen` 入口与阅读菜单/抽屉 Toc 流进入，`MainNavKey`/`MainNavGraph` 聚合；预览/选章结果载荷走官方 `ResultEffect`（`NavResults.kt`，自定义 `newNavResultKey` 防多 opener 串台）。调用方与 Manifest 零改动。**M1-3y（2026-09-13）转 KMP/CMP**：Contract / 纯 Screen / VM 进 `commonMain`，新拆 `androidMain` 的 `TxtRuleRouteScreen`（`koinViewModel()` + `rememberDocumentPicker()` + `koinInject<Toaster>()`）；26 条文案搬 `composeResources`（`zh-rHK`/`zh-rTW` 合法各少 3 条，与 `:app` res 的缺失模式一致）；VM 不再继承吃 `Application` 的 `BaseRuleViewModel`（导入/导出/上传下沉 `RuleTransferUseCase`）；新增平台契约 `TxtTocRuleImportCompat`——`TxtTocRule.chapterRule` 的旧键名 `rule` 兼容已改由 `GSON` 门面的 deserializer 承担，共享层 `JsonCodec` 不含它，故**标准 JSON 也走平台**；根登记 `"feature/txttocrules" to "cmp"`。 |
| `ui/book/bookmark`                       | `feature/bookmarks`         | Feature                       | 与阅读入口通过 route/result contract 协作                                             |
| `ui/book/group`、`ui/book/manage`         | `feature/book-management`   | Feature                       | 先收拢管理动作，再决定书籍分组是否形成独立契约                                                      |
| `ui/book/import`、`ui/file` 中导入流程         | `feature/book-import`       | Feature + platform adapter    | 文件选择/URI 留 Android adapter，导入状态与规则归 Feature/domain                           |
| `ui/book/cache`                          | `feature/book-cache`        | Feature                       | 缓存任务执行由 domain/service 能力提供                                                  |
| `ui/book/changecover`                    | `feature/change-cover`      | Feature                       | 图片选择与裁剪由 host/platform effect 处理                                             |
| `ui/book/changesource`、`ui/book/source`  | `feature/book-source`       | Feature                       | source edit/manage/debug 可作为同域子包，避免交叉 impl 依赖                                |
| `ui/book/readRecord`                     | `feature/reading-history`   | Feature                       | 新包统一全小写；不要延续 camelCase package                                               |
| `ui/book/readaloud`                      | `feature/read-aloud`        | Feature + platform adapter    | 控制 UI 可 Feature 化，TTS/service/media 生命周期留 platform                           |
| `ui/book/audio`                          | `feature/audio-playback`    | Feature + platform adapter    | 播放服务、媒体会话和通知留 Android platform                                               |
| `ui/book/read`                           | `platform/android/reader`   | platform island               | 当前不作为普通 Feature 搬迁；共享状态/模型另行立项                                               |
| `ui/book/manga`                          | `feature/manga`             | Feature + renderer capability | 图片渲染、缓存和手势需专项边界审计                                                            |
| `ui/config/*`                            | `feature/settings`          | Feature domain                | **`:feature:settings` 已建（M5-2a），按子页分片填充。截至 **M5-16a** 已迁入 `nav/`（域首页）、`lab/`、`translation/`、`ai/`（域收官）、`customtheme/`、`downloadcache/`（已抽 `DownloadCachePlatform`）、`readconfig/`（`ReadConfigContract`+VM M5-11a、`PageKeySheet` M5-11b、`ClickActionConfigSheet` M5-11c、页面本体 M5-11d；`EyeProtectionConfigSheet` M5-10a）、`coverconfig/`（M5-12〜M5-14，**子域收官**）、`thememanage/`（`ThemeManagePlatform`+VM M5-16a）。⚠️ **`:app` 的 `ui/config` 实测剩 17 个文件**（2026-09-24 实测，不估算）：`themeConfig`(7；合同 M5-19b、`BackgroundImageManageSheet` M5-19a、两个导航 sheet M5-19c 已迁；余下 `LabelColorManageSheet` / `LauncherIconPickerSheet` / VM / Screen / `ThemeConfig.kt` / 宿主壳) / `coverConfig`(3：`CoverConfigRouteScreen` + 两个 sheet) / `readConfig`(2) / `otherConfig`(2，均为宿主壳) / `backupConfig`(1 宿主壳) / `themeManage`(1：`ThemeManageRouteScreen` 宿主壳；VM M5-16a、测试 M5-16b、表单 M5-17a、页面 M5-18 均已迁) / `bookshelfConfig`(1)（`downloadCacheConfig` 已归零）。多数要先抽 1–2 个平台契约（`WebService` / `Permissions` / `ImportOldData` / `SavedTheme` / `ThemePackageManager` / `BookCover`…）；`themeConfig` 成本最高（撞 `ui.main.*` 的 10 个 `Launcher*` 图标）。**根包只剩 `ConfigTag.kt`**（只被 `:app` 的 `MainIntent` 用于 deep-link 路由 tag 映射） |
| `ui/theme`                               | `core/designsystem`         | core candidate                | theme engine、token 与基础主题；Feature 专属样式不进入 core                                |
| `ui/widget/components`                   | owner audit                 | core/ui candidate             | 有至少两个 Feature 调用且职责稳定的组件才进入 `core/ui` 或 `core/designsystem`                  |
| `ui/widget` 其余 View-era 控件               | legacy/platform owner       | migration zone                | Recycler/View/Dialog 控件按实际调用方迁走，禁止整目录改名为 core                                |
| `ui/rss/*`、`ui/main/rss`                 | `feature/rss`               | Feature domain                | 初期一个业务域，内部 `feed`、`article`、`source`、`subscription` 子包                       |
| `feature/replacerules/{,edit}`（原 `ui/replace`） | `feature/replace-rules` | Feature domain | **Stage B 完成（2026-09-09）**：`:feature:replacerules`（Android library + Compose），7 文件（ReplaceRuleScreen/Contract/VM + edit/…）+ `ReplaceRuleRepository`（下沉 `core:data/commonMain`）+ 52 R.string×4 语言。`io.legado.app.feature.replacerules[.edit]`。`:app` 仅剩 DI（`appModule`）、host 导航（`MainNavKey`/`MainNavGraph`）、调用点。原 catalog 记的「reader/toc/my 直接 import `ReplaceEditRoute`」已收口为 host 导航契约；`MainRouteReplaceRule` 是 route，非跨 Feature import。验证：模块 `compileDebugKotlin`（Feature 模块不跑 lint，对齐 tagrules）。**M1-3x（2026-09-13）转 KMP/CMP**：8 文件进 `commonMain` + 2 个新拆 Route 进 `androidMain`，52 文案×4 语言进 `composeResources`；`ReplaceAnalyzer` 搬到 `:core:data` 的 `io.legado.app.data.rules`、`AndroidReplaceRuleImportCompat` 住 `domain/gateway`；`ReplaceRuleStateTest` 从 `:app/src/test` 搬进本模块 `androidHostTest`；根登记 `"feature/replacerules" to "cmp"` |
| `ui/dict`、`ui/dict/rule` | `feature/dict` | Feature + platform adapter | **拆两域 + ①已模块化（2026-09-09）**：① `rule` 子域 = 词典规则管理，**`:feature:dict` 已建**：`DictRuleScreen`（含 `DictRuleRouteScreen`）/`DictRuleContract`/`DictRuleViewModel` 迁入 `io.legado.app.feature.dict.rule`（3 文件 + 19 R.string×4 语言），结构逐文件同构 tagrules 的 HighlightTagRule 页；宿主 `DictRuleActivity` **已 nav3 化（2026-09-10 删除）**——原为依赖 `BaseComposeActivity` 的薄宿主（仅 import 模块 `DictRuleRouteScreen`），收口改走同栈 `MainRouteDictRule`（data object，纯管理页无 picker），经 `MyScreen` 入口进入，`MainNavKey`/`MainNavGraph` 聚合，Manifest 条目删除。② `dict` 主包 = 词典查询弹窗面板（DictActivity/DictSheet/DictViewModel），其 `DictRule.search` 走 app 独有 `DictRuleAndroid.kt`（依赖 `model.analyzeRule.AnalyzeRule`/`AnalyzeUrl`）→ **platform island，不迁**，待 search 抽平台注入契约。`ExportSelection.uri` 已去 `android.net.Uri`（→String）。验证：`:feature:dict:compileDebugKotlin` + `:app:compileAppDebugKotlin` 通过（模块不跑 lint，对齐 tagrules）。**③ 已转 KMP/CMP（M1-3z，2026-09-13）**：3 源文件进 `commonMain`（Contract 未改 / 纯 Screen / 重写 VM），`DictRuleRouteScreen` 单独拆进 `androidMain`（`koinViewModel()` + `rememberDocumentPicker()` 是 Android-only），19 条文案 ×4 语言进 `composeResources`（资源包名 `io.legado.app.feature.dict.res`，**不含 `rule` 子包**），根登记 `cmp`。**不需要平台契约**——`GSON` 门面注册的自定义 deserializer 不含 `DictRule`，`JsonCodec` 与 `GSON` 对本类型等价。剪贴板：VM 的 `ClipboardProvider.current` → 注入 `Clipboard`（Screen 的 `LocalClipboard.current` 是 CMP 共享 API，保留）；G4 清 3 条旧账（gson 1 / legacyBase 2 / coreProvider 3）。验证：`:feature:dict:compileKotlinDesktop` + 四门禁 + 全量集通过。剩余：`ImportDictRuleViewModel`（`ui/association`）随依赖评估是否入模块 |
| `feature/tagrules/{highlight,group}`（原 `ui/highlightTagRule`、`ui/tagGroupRule`） | `feature/tag-rules` | Feature domain | **Stage B 完成（2026-09-08）**：`:feature:tagrules`（Android library + Compose），7/7 文件迁入（含 `highlight/HighlightTagRuleScreen`），`ImportComponents`（Gson JSON 树）入 `:core:ui`。Contract 已去平台化（Uri→String），规则 VM 注入 Repository，删除零引用类型 `TagGroupRuleEffect`/两个 `*RenderState`；`android.net.Uri.parse`/`context.toastOnUi` 已随 `BaseRuleViewModel` 去平台交互契约化（`:core:platform` 的 `RuleTransferPlatform`/`ToasterProvider`）清零。`:app` 仅剩 DI、host 导航、调用点。**M1-3w（2026-09-13）转 KMP/CMP**：7 源文件拆源集（`commonMain` 6 + `androidMain` 的 `HighlightTagRuleRouteScreen`），31 条文案×4 语言从 `src/main/res` 搬进 `composeResources`，测试进 `androidHostTest`；根登记 `"feature/tagrules" to "cmp"`（**首个转 CMP 的 Feature**） |
| `ui/ai/chat`                             | `feature/ai-chat`           | Feature                       | AI provider/config 归 settings 或 data/platform，不放 Screen                      |
| `ui/login`                               | `feature/source-login`      | Feature + host effect         | WebView/cookie/platform auth 通过 adapter                                      |
| `ui/about`                               | `feature/about`             | Feature + platform adapter    | **原备注「边界小，适合首个样板候选」已被实测证伪（2026-09-16 审计）**：本体 7 文件/1442 行，但依赖闭包牵出 `TextCard`（39 引用方）/`MarkdownBlock`（894 行，含 `Intent`+Splitties 剪贴板）/`CrashLogSheet`（`FileDoc`），`FileDoc`（23 引用方、深 SAF 依赖）**不迁**，外加 VM 三组平台能力（更新检查/诊断/assets 读 md）。审计见 [feature-slicing-audit-about.md](./feature-slicing-audit-about.md)。**`MiuixAboutScreen`（519 行）判为 platform island 留 `:app`**——它直接调 `miuix.kmp.blur.*`，而 `miuix-blur` 只有 `-android` 制品、无 desktop 变体（编译探测 Unresolved）；由 androidMain 的Route 按 `ThemeResolver.isMiuixEngine` 分流，两支复用同一 ViewModel/Contract。连带 `MiuixUtils` 与 `BgEffect*` **不必搬**（只有 miuix 屏用）。**M5-1a 已完成**：`TextCard` 上提 `designsystem/commonMain`（`git mv`，包名不变 ⇒ 39 个调用方import 零改动）；验证＝designsystem desktop 编译 + `:core:ui`/`:app` 编译 + 四门禁（无需下调基线）+ 干净重建全量集，计数不变。**M5-1b/c 已完成（2026-09-21）**：`:feature:about` 建层并**接管运行路径**——`commonMain` 装 Contract/VM/`MaterialAboutScreen`+`AboutOverlays`/`UpdateSheet`/`CrashLogSheet`/`CrashReportScreen` + 35 文案×4 语言 + 4 个重建的 `ImageVector` 图标；三个平台能力（`AppUpdateChecker` / `AboutDiagnostics` / `BundledTextReader`）的 Android 实现留在 `io/legado/app/platform`，desktop 侧显式 `UnsupportedOperationException`。**分流与 Route 留在 `:app`**（`MainNavGraph` 收 Effect + 按 `isMiuixEngine` 二选一），故 `:feature:about` 的 `androidMain` 没有代码——这是它与四个规则 Feature 唯一的形态差异。`:app` 的 `ui/about` 只剩 `MiuixAboutScreen` + `CrashReportActivity`（后者是 `CrashHandler` 硬编码的 ABI）。实录见审计文档 §5–§8。**未验证**：about 页运行期行为无自动化覆盖，需真机冒烟；且实测「Koin 漏绑不导致编译失败」，宿主 graph creation test 仍是缺口。 |
| `ui/welcome`                             | `feature/onboarding`        | Feature                       | 首次启动状态经 settings/domain contract                                             |
| `ui/qrcode`                              | `feature/qr-scan`           | Feature + Android capability  | Camera/permission/result 留 host/platform                                     |
| `ui/association`                         | `feature/file-association`  | Feature + Android capability  | 外部 Intent/URI 是兼容入口，不进入共享 contract                                           |
| `ui/browser`                             | `platform/android/browser`  | platform capability           | 若出现独立用户流程再由 Feature 包装，不把 WebView 放 core UI                                  |

## Catalog 使用规则

- 一项只能有一个 canonical owner；子流程通过子包表达，不复制完整 Feature 栈。
- 发现当前包横跨多个 owner 时，先画依赖和入口，不直接移动。
- owner 变更必须同步本表、route/DI 聚合位置和迁移 PR 的回滚说明。
- 新 Feature 名先登记再落文件，避免同义目录继续增长。
- Feature 提升为 Gradle module 或 KMP module 后，在备注中记录真实 module path、source sets 与验证
  task。
